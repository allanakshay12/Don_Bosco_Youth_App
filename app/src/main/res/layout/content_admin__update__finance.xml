<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.allanakshay.donboscoyouth.Admin_Update_Finance"
    tools:showIn="@layout/activity_admin__update__finance"
    android:background="@color/black_overlay">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Add Finance"
        android:textColor="@color/background"
        android:textStyle="bold"
        android:background="@drawable/attendance_add"
        android:id="@+id/financeupdate"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/financeadmin_list"
        android:divider="@color/black_overlay"
        android:dividerHeight="5dp"
        android:layout_above="@id/financeupdate">
    </ListView>

</RelativeLayout>
