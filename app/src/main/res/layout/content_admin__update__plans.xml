<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.allanakshay.donboscoyouth.Admin_Update_Plans"
    tools:showIn="@layout/activity_admin__update__plans"
    android:background="@color/black_overlay">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Add Plan"
        android:textColor="@color/background"
        android:textStyle="bold"
        android:background="@drawable/attendance_add"
        android:id="@+id/planupdate"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@color/black_overlay"
        android:id="@+id/plan_list"
        android:dividerHeight="5dp"
        android:layout_above="@id/planupdate">
    </ListView>

</RelativeLayout>
